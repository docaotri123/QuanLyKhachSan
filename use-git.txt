git config --global user.name "docaotri123"
git clone https://github.com/docaotri123/test.git

cd test(thu muc hien hanh)

git add .

git status

git commit -m "Lan comit thu n"

git tag versionn

git push -u origin master // cap nhat branch master

git push origin RELEASE-bugrep //de d?y tag n?p bài lên github

///
git pull

